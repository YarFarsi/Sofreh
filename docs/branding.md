# Branding

Product name: **سفره (Sofreh)** — a sofreh is the cloth you spread for a meal.

Organizations can set the display name in **Settings → نام سامانه** (`orgNameFa`). The default is «سفره».

Replace files in `public/` for a logo. Keep application code unchanged when only branding is needed.
